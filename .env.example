# Variables de entorno para Railway
# Railway automáticamente inyecta DATABASE_URL cuando conectas una base de datos PostgreSQL

# URL de conexión a PostgreSQL (Railway la provee automáticamente)
DATABASE_URL=postgresql://username:password@host:port/database

# Puerto del servidor (Railway lo provee automáticamente, local usar 3000)
PORT=3000

# Entorno de ejecución
NODE_ENV=production
